border:1px solid #E7E5E4;border-radius:12px;font:600 13px Tajawal,sans-serif\">كيف أستعيد كلمة المرور؟</div>\n  <div style=\"flex:1\"></div>\n  <button class=\"pbtn\" style=\"background:#25D366\">💬 تواصل عبر واتساب</button>\n  <button class=\"sbtn\">📞 اتصال بالدعم</button>\n</div>\n</sc-if>\n\n</div>\n\n<sc-if value=\"{{ is.tabbed }}\" hint-placeholder-val=\"{{ false }}\">\n<div style=\"display:flex;justify-content:space-around;padding:10px 8px 14px;border-top:1px solid #F5F5F4;background:#fff\">\n  <div onClick=\"{{ nav_home }}\" style=\"display:flex;flex-direction:column;align-items:center;gap:3px;color:{{ tabColor.home }};cursor:pointer\"><span>🏠</span><span style=\"font:700 10px Tajawal,sans-serif\">الرئيسية</span></div>\n  <div onClick=\"{{ nav_orders }}\" style=\"display:flex;flex-direction:column;align-items:center;gap:3px;color:{{ tabColor.orders }};cursor:pointer\"><span>🧾</span><span style=\"font:600 10px Tajawal,sans-serif\">طلباتي</span></div>\n  <div onClick=\"{{ nav_profile }}\" style=\"display:flex;flex-direction:column;align-items:center;gap:3px;color:{{ tabColor.profile }};cursor:pointer\"><span>👤</span><span style=\"font:600 10px Tajawal,sans-serif\">الحساب</span></div>\n</div>\n</sc-if>\n\n<sc-if value=\"{{ toast.show }}\" hint-placeholder-val=\"{{ false }}\">\n<div style=\"position:absolute;bottom:100px;left:20px;right:20px;padding:12px 16px;background:#1C1917;color:#fff;border-radius:12px;font:600 12px Tajawal,sans-serif;text-align:center;box-shadow:0 8px 20px rgba(0,0,0,.3)\">{{ toast.msg }}</div>\n</sc-if>\n\n</div>\n</div>\n</div>\n\n</x-dc>\n<script type=\"text/x-dc\" data-dc-script>\nclass Component extends DCLogic {\n  state = {\n    screen: 'splash',\n    history: [],\n    lang: 'ar',\n    otp: ['', '', '', ''],\n    forgotStep: 0,\n    rating: 0,\n    cart: [],\n    selRestaurant: 0,\n    selDish: null,\n    dishQty: 1,\n    notifOn: true,\n    toastMsg: ''\n  };\n\n  restaurants = [\n    { key: 'wadi', name: 'مطعم الوادي', cuisine: 'مأكولات موريتانية', rating: '4.6', eta: '25-35 د', min: '1000', img: '🍛',\n      dishes: [ { name: 'تشبوجين بالسمك', price: 1800 }, { name: 'أرز باللحم', price: 1600 }, { name: 'سلطة موريتانية', price: 600 }, { name: 'شاي أتاي', price: 300 } ] },\n    { key: 'pizza', name: 'بيتزا نواكشوط', cuisine: 'إيطالي', rating: '4.4', eta: '20-30 د', min: '1500', img: '🍕',\n      dishes: [ { name: 'بيتزا مارغريتا', price: 2200 }, { name: 'بيتزا دجاج', price: 2600 }, { name: 'عصير طبيعي', price: 500 } ] },\n    { key: 'plage', name: 'مطعم الشاطئ', cuisine: 'مأكولات بحرية', rating: '4.7', eta: '30-40 د', min: '1200', img: '🐟',\n      dishes: [ { name: 'سمك مقلي', price: 2400 }, { name: 'أرز بالمأكولات البحرية', price: 2800 }, { name: 'سلطة خضراء', price: 500 } ] }\n  ];\n\n  nav(screen) {\n    this.setState(s => ({ history: [...s.history, s.screen], screen }));\n  }\n  back = () => {\n    this.setState(s => {\n      const h = [...s.history];\n      const prev = h.pop() || 'home';\n      return { history: h, screen: prev };\n    });\n  };\n  toast(msg) {\n    this.setState({ toastMsg: msg });\n    setTimeout(() => this.setState({ toastMsg: '' }), 1800);\n  }\n\n  renderVals() {\n    const s = this.state;\n    const dir = (s.lang === 'fr' && ['home', 'foodList', 'taxiConfirm'].includes(s.screen)) ? 'ltr' : 'rtl';\n    const backArrow = dir === 'rtl' ? '→' : '←';\n    const chevron = dir === 'rtl' ? '‹' : '›';\n    const screens = ['splash','lang','onboarding','login','signup','otp','forgot','permLocation','permNotif','home','homeEmpty','taxiPickup','taxiDest','taxiConfirm','taxiSearching','taxiNoDriver','taxiFound','taxiTracking','taxiEnd','taxiRate','foodList','foodEmpty','foodDetail','foodDish','foodCart','foodConfirm','foodWaiting','foodRejected','foodTracking','foodEnd','voiceRecord','voiceAnalyzing','voiceConfirm','voiceFail','orders','profile','settings','notifications','support'];\n    const is = {};\n    screens.forEach(k => is[k] = s.screen === k);\n    is.chrome = !['splash'].includes(s.screen);\n    is.tabbed = ['home', 'homeEmpty', 'orders', 'profile'].includes(s.screen);\n\n    const onboardSlides = [\n      { icon: '🚕', title: 'تكسي في متناول يدك', desc: 'اطلب سيارة إلى أي مكان في المدينة في ثوانٍ معدودة.' },\n      { icon: '🍽️', title: 'طعامك المفضل يصلك', desc: 'اكتشف أفضل المطاعم في نواكشوط واطلب توصيل الطعام بسهولة.' },\n      { icon: '📦', title: 'توصيل سريع وآمن', desc: 'أرسل واستلم طرودك عبر شبكة موصّلين موثوقين.' }\n    ];\n    const obStep = s.onboardStep || 0;\n    const ob = onboardSlides[obStep];\n    const onboard = {\n      icon: ob.icon, title: ob.title, desc: ob.desc,\n      d0: obStep === 0 ? '22px' : '6px', d1: obStep === 1 ? '22px' : '6px', d2: obStep === 2 ? '22px' : '6px',\n      btn: obStep === 2 ? 'ابدأ الآن' : 'التالي',\n      next: () => obStep === 2 ? this.nav('login') : this.setState({ onboardStep: obStep + 1 })\n    };\n\n    const cart = s.cart.map((c, i) => ({\n      name: c.name, price: c.price, qty: c.qty,\n      inc: () => this.setState(st => { const nc = [...st.cart]; nc[i] = { ...nc[i], qty: nc[i].qty + 1 }; return { cart: nc }; }),\n      dec: () => this.setState(st => { const nc = [...st.cart]; if (nc[i].qty > 1) nc[i] = { ...nc[i], qty: nc[i].qty - 1 }; else nc.splice(i, 1); return { cart: nc }; })\n    }));\n    const cartCount = s.cart.reduce((a, c) => a + c.qty, 0);\n    const cartTotal = s.cart.reduce((a, c) => a + c.qty * c.price, 0);\n    const cartGrandTotal = cartTotal + (s.cart.length ? 300 : 0);\n\n    const restaurant = this.restaurants[s.selRestaurant];\n    const sel = {\n      img: restaurant.img, name: restaurant.name, rating: restaurant.rating, eta: restaurant.eta,\n      dishes: restaurant.dishes.map((d, i) => ({\n        name: d.name, price: d.price,\n        open: () => this.setState({ selDish: i, dishQty: 1 }, () => this.nav('foodDish')),\n        add: () => this.setState(st => ({ cart: [...st.cart, { name: d.name, price: d.price, qty: 1 }] }))\n      }))\n    };\n    const dishRaw = s.selDish != null ? restaurant.dishes[s.selDish] : { name: '', price: 0 };\n    const dish = {\n      name: dishRaw.name, price: dishRaw.price, qty: s.dishQty,\n      inc: () => this.setState(st => ({ dishQty: st.dishQty + 1 })),\n      dec: () => this.setState(st => ({ dishQty: Math.max(1, st.dishQty - 1) })),\n      addAndBack: () => { this.setState(st => ({ cart: [...st.cart, { name: dishRaw.name, price: dishRaw.price, qty: st.dishQty }] })); this.nav('foodDetail'); }\n    };\n\n    const isFr = s.lang === 'fr';\n    const tc = isFr && s.screen === 'taxiConfirm' ? {\n      title: 'Confirmer la course', mapLabel: 'Trajet sur la carte', etaText: '18 min', priceLabel: 'Prix estimé',\n      vehicleLabel: 'Véhicule', paymentLabel: 'Paiement', cashLabel: 'Espèces', notePh: 'Note pour le chauffeur (optionnel)', cta: 'Commander maintenant'\n    } : {\n      title: 'تأكيد الرحلة', mapLabel: 'المسار على الخريطة', etaText: '18 دقيقة', priceLabel: 'السعر التقديري',\n      vehicleLabel: 'نوع المركبة', paymentLabel: 'طريقة الدفع', cashLabel: 'نقدًا', notePh: 'ملاحظة للسائق (اختياري)', cta: 'اطلب الآن'\n    };\n    const fl = isFr && s.screen === 'foodList' ? {\n      title: 'Restaurants près de vous', searchPh: 'Rechercher un restaurant...', f1: 'Le mieux noté', f2: 'Le plus rapide', f3: 'Cuisine', minLabel: 'Min',\n      restaurants: this.restaurants.map((r, i) => ({ name: r.key === 'wadi' ? 'Restaurant El Wadi' : r.key === 'pizza' ? 'Pizza Nouakchott' : 'Restaurant La Plage', cuisine: r.cuisine, rating: r.rating, eta: r.eta, min: r.min, img: r.img, open: () => this.setState({ selRestaurant: i }, () => this.nav('foodDetail')) }))\n    } : {\n      title: 'مطاعم قريبة منك', searchPh: 'بحث عن مطعم...', f1: 'الأعلى تقييمًا', f2: 'الأسرع توصيلًا', f3: 'نوع المطبخ', minLabel: 'الحد الأدنى',\n      restaurants: this.restaurants.map((r, i) => ({ name: r.name, cuisine: r.cuisine, rating: r.rating, eta: r.eta, min: r.min, img: r.img, open: () => this.setState({ selRestaurant: i }, () => this.nav('foodDetail')) }))\n    };\n\n    const home = isFr ? {\n      greetLabel: 'Bonjour', userName: 'Fatima', langBtn: 'AR', city: 'Nouakchott', pinSide: 'right',\n      taxiTitle: 'Taxi', taxiDesc: 'Une course en quelques secondes', foodTitle: 'Food', foodDesc: 'Vos plats préférés livrés',\n      deliveryTitle: 'Livraison', deliveryDesc: 'Envoyez un colis rapidement', voiceLabel: 'Commander à la voix'\n    } : {\n      greetLabel: 'مساء الخير', userName: 'فاطمة', langBtn: 'FR', city: 'نواكشوط', pinSide: 'left',\n      taxiTitle: 'تكسي', taxiDesc: 'رحلة في ثوانٍ معدودة', foodTitle: 'طعام', foodDesc: 'أطباقك المفضلة تصلك بسرعة',\n      deliveryTitle: 'توصيل', deliveryDesc: 'أرسل طردك بسرعة وأمان', voiceLabel: 'اطلب بصوتك'\n    };\n\n    const foodStepDefs = [\n      { label: 'تم قبول الطلب', done: true }, { label: 'قيد التحضير', done: true },\n      { label: 'جاهز للاستلام', done: true }, { label: 'في الطريق مع المندوب', done: false }, { label: 'تم التسليم', done: false }\n    ];\n    const foodSteps = foodStepDefs.map((st, i) => ({\n      label: st.label, mark: st.done ? '✓' : '', bg: st.done ? '#16A34A' : '#E7E5E4',\n      textColor: st.done ? '#1C1917' : '#A8A29E', showLine: i < foodStepDefs.length - 1, lineColor: st.done ? '#16A34A' : '#E7E5E4'\n    }));\n\n    const rateColor = {}; for (let i = 1; i <= 5; i++) rateColor[i] = i <= s.rating ? '#FACC15' : '#E7E5E4';\n\n    return {\n      dir, backArrow, chevron, is, lang: s.lang, otp: { 0: s.otp[0], 1: s.otp[1], 2: s.otp[2], 3: s.otp[3] }, otpTimer: 30,\n      onboard, home, tc, fl, sel, dish,\n      driver: { name: 'محمد ولد أحمد', rating: '4.8', car: 'Toyota Corolla · أبيض', plate: 'نواكشوط 4521', eta: '3 دقائق' },\n      hasCart: s.cart.length > 0, hasCartNot: s.cart.length === 0, cart, cartCount, cartTotal, cartGrandTotal,\n      foodSteps, rateColor,\n      demoVisible: true,\n      notifOnColor: s.notifOn ? '#16A34A' : '#D6D3D1', notifJustify: s.notifOn ? 'flex-end' : 'flex-start',\n      toast: { show: !!s.toastMsg, msg: s.toastMsg },\n      tabColor: { home: is.home || is.homeEmpty ? '#16A34A' : '#A8A29E', orders: is.orders ? '#16A34A' : '#A8A29E', profile: is.profile ? '#16A34A' : '#A8A29E' },\n      forgot: { step0: s.forgotStep === 0, step1: s.forgotStep === 1, step2: s.forgotStep === 2, toStep1: () => this.setState({ forgotStep: 1 }), toStep2: () => this.setState({ forgotStep: 2 }) },\n\n      resetApp: () => this.setState({ screen: 'splash', history: [], cart: [], onboardStep: 0, forgotStep: 0, rating: 0 }),\n      nav_lang: () => this.nav('lang'), pick_ar: () => { this.setState({ lang: 'ar' }); this.nav('onboarding'); }, pick_fr: () => { this.setState({ lang: 'fr' }); this.nav('onboarding'); },\n      nav_login: () => this.nav('login'), nav_signup: () => this.nav('signup'), nav_otp: () => this.nav('otp'), nav_forgot: () => this.nav('forgot'),\n      nav_permLocation: () => this.nav('permLocation'), nav_permNotif: () => this.nav('permNotif'), nav_home: () => this.setState({ screen: 'home', history: [] }),\n      nav_homeEmpty: () => this.nav('homeEmpty'),\n      nav_taxiPickup: () => this.nav('taxiPickup'), nav_taxiDest: () => this.nav('taxiDest'), nav_taxiConfirm: () => this.nav('taxiConfirm'),\n      nav_taxiSearching: () => this.nav('taxiSearching'), nav_taxiNoDriver: () => this.nav('taxiNoDriver'), nav_taxiFound: () => this.nav('taxiFound'),\n      nav_taxiTracking: () => this.nav('taxiTracking'), nav_taxiEnd: () => this.nav('taxiEnd'), nav_taxiRate: () => this.nav('taxiRate'),\n      nav_foodList: () => this.nav('foodList'), nav_foodEmpty: () => this.nav('foodEmpty'), nav_foodDetail: () => this.nav('foodDetail'),\n      nav_foodCart: () => this.nav('foodCart'), nav_foodConfirm: () => this.nav('foodConfirm'), nav_foodWaiting: () => this.nav('foodWaiting'),\n      nav_foodRejected: () => this.nav('foodRejected'), nav_foodTracking: () => this.nav('foodTracking'), nav_foodEnd: () => this.nav('foodEnd'),\n      nav_voiceRecord: () => { this.setState({ voiceSec: 0 }); this.nav('voiceRecord'); }, nav_voiceAnalyzing: () => this.nav('voiceAnalyzing'),\n      nav_voiceConfirm: () => this.nav('voiceConfirm'), nav_voiceFail: () => this.nav('voiceFail'),\n      nav_orders: () => this.setState({ screen: 'orders', history: [] }), nav_profile: () => this.setState({ screen: 'profile', history: [] }),\n      nav_settings: () => this.nav('settings'), nav_notifications: () => this.nav('notifications'), nav_support: () => this.nav('support'),\n      back: this.back,\n      toggleLang: () => this.setState(st => ({ lang: st.lang === 'ar' ? 'fr' : 'ar' })),\n      toggleNotif: () => this.setState(st => ({ notifOn: !st.notifOn })),\n      showToast: () => this.toast('تم إرسال رابط تتبّع الرحلة'),\n      showDeliverySoon: () => this.toast('خدمة التوصيل — قريبًا في هذا العرض'),\n      setOtp0: e => this.setState(st => { const o = [...st.otp]; o[0] = e.target.value; return { otp: o }; }),\n      setOtp1: e => this.setState(st => { const o = [...st.otp]; o[1] = e.target.value; return { otp: o }; }),\n      setOtp2: e => this.setState(st => { const o = [...st.otp]; o[2] = e.target.value; return { otp: o }; }),\n      setOtp3: e => this.setState(st => { const o = [...st.otp]; o[3] = e.target.value; return { otp: o }; }),\n      setRate1: () => this.setState({ rating: 1 }), setRate2: () => this.setState({ rating: 2 }), setRate3: () => this.setState({ rating: 3 }),\n      setRate4: () => this.setState({ rating: 4 }), setRate5: () => this.setState({ rating: 5 }),\n      voiceSec: s.voiceSec || 0\n    };\n  }\n}\n\n</script>\n</body>\n</html>\n","contentType":"text/html","isBase64":false,"truncated":false}