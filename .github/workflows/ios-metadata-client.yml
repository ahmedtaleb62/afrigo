name: iOS Metadata - Client

on:
  workflow_dispatch:

jobs:
  metadata:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/afrigo_client/ios

    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
          working-directory: apps/afrigo_client/ios

      - name: Push App Store Connect metadata
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_CONTENT: ${{ secrets.ASC_KEY_CONTENT }}
          ASC_DEMO_USER: ${{ secrets.ASC_DEMO_USER_CLIENT }}
          ASC_DEMO_PASSWORD: ${{ secrets.ASC_DEMO_PASSWORD_CLIENT }}
        run: bundle exec fastlane metadata
