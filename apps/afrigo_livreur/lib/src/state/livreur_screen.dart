/// Every screen in the Livreur app (screens 73-82 of the design). Mirrors
/// the `screen` field of the original design prototype's state machine 1:1.
enum LivreurScreen {
  splash,
  login,
  signup,
  otp,
  vehicleDocs,
  pendingApproval,
  rejected,
  home,
  wallet,
  navigateToPickup,
  navigateToDropoff,
  deliveryEnd,
  rateCustomer,
  history,
  profile,
}
