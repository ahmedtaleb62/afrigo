# Copy to .env and fill in (see packages/afrigo_core README for how these
# are consumed). SUPABASE_ANON_KEY is safe to ship in the client binary —
# it is not a secret, access is enforced by RLS.
SUPABASE_URL=https://ecbxpcxjfvlobduapctu.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
GOOGLE_MAPS_API_KEY=
